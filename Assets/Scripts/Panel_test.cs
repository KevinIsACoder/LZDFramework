using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
//Author : #AUTHOR#
//CreateDate : #DATETIME#
//DESC : #DESC#
public class Panel_test : MonoBehaviour {
	public Image image;
	// Use this for initialization
	void Start () {
		// string bundlePath = Application.streamingAssetsPath + "/" + "circle.assetbundle";
		// AssetBundle bundle = AssetBundle.LoadFromFile(bundlePath);
		// Sprite sprite = bundle.LoadAsset<Sprite>("00002");
		// image.sprite = sprite;
		// bundle.Unload(false);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
