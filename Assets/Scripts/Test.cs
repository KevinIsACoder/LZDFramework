using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System;
public class Test : MonoBehaviour {

	// Use this for initialization
	void Start () {
		string str = "lua";
		bool index = str.Contains("lu");
		Debug.Log(index);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
